package com.igorini.togepibot.properties

/* Contains the names of database related properties. Values of these properties are stored in koin.properties */

class DatabaseProperties {
    companion object {
        const val url = "togepibot.db.url"
        const val driver = "togepibot.db.driver"
        const val username = "togepibot.db.username"
        const val password = "togepibot.db.password"
    }
}
